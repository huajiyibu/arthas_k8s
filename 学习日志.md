# 🎓 学习日志：Arthas 注入 + 诊断命令 API 服务项目

> 记录人：我（Java 零基础，Python 会一点，K8s 会一点，Arthas 零基础）
> 开始日期：2026-08-03
> 目标：把"手动向 K8s Pod 注入 Arthas + 敲诊断命令排查性能"这件事，做成一个 Python + FastAPI 的自动化 API 服务。

---

## 📌 当前进度

| 阶段 | 内容 | 状态 |
|---|---|---|
| 阶段 0 | 环境准备 | ✅ 完成 |
| 阶段 1 | 本地玩转 Arthas（trace/monitor/watch） | ✅ 完成 |
| 阶段 2 | 理解 K8s 手动注入 Arthas | ✅ 完成 |
| 阶段 3 | Python 封装 kubectl（注入三步） | ✅ 完成 |
| 阶段 4 | FastAPI + 注入 API | ✅ 完成 |
| 阶段 5 | 4 个诊断命令 API | ✅ 完成 |
| 阶段 6 | 完善（批量/超时/异常） | ✅ 完成 |
| 阶段 5 | 4 个诊断命令 API | ⬜ 未开始 |
| 阶段 6 | 完善与收尾（批量/超时/异常） | ⬜ 未开始 |

---

## 🗓️ 详细过程记录

### 阶段 0：环境准备与摸底（2026-08-03）

**做了什么**
- 摸底本机环境：Java 1.8、kubectl v1.34.1、Python 3.13.14 都装了
- 发现 kubectl 没有激活的集群上下文（`current-context is not set`）
- 制定了 7 阶段学习+开发路线图

**学到的**
- 本项目本质：用 API 封装"注入 Arthas + 执行诊断命令"，Java 应用只需能跑、能被 attach，不需要我懂 Java。

---

### 阶段 1：本地玩转 Arthas（2026-08-03）

**1.1 让"病人"程序跑起来**
- 用 `java -jar math-game.jar` 启动演示程序（不停做质因数分解，如 `156248=2*2*2*19531`，还会故意传负数制造异常）
- 理解：Arthas 必须"挂"到一个**正在运行**的 Java 程序上才能诊断

**1.2 踩坑：JRE vs JDK（重要！）**
- 症状：`arthas-boot.jar` 报 `Can not find java process`，还建议我运行 `jps` 命令
- 排查：`jps -l` 报命令不存在；`java -version` 显示 `Runtime Environment`（说明用的是 JRE）；`C:\Program Files\Java` 里其实装了 JRE + JDK 两个
- 结论：Arthas 靠 `jps` 列进程、靠 attach 机制挂载，这些工具**只有 JDK 才有**；系统默认用了 JRE 所以找不到
- 解决：把用户级 `JAVA_HOME` 和 PATH 指向 `C:\Program Files\Java\jdk1.8.0_341`（完整 JDK）

**1.3 踩坑：VS Code 环境变量缓存**
- 症状：配置完环境变量后，新开的终端 `jps` 还是找不到
- 原因：VS Code 在启动那一刻读取环境变量，新终端标签页继承旧环境，不重新读注册表
- 解决：当前终端用 `$env:Path = "C:\Program Files\Java\jdk1.8.0_341\bin;" + $env:Path` 临时刷新；永久生效需重启 VS Code
- **这是 Windows 开发超常见的坑，已学会识别**

**1.4 成功 attach 进入 Arthas**
- 用 JDK 的 java 启动 `arthas-boot.jar` → 列出进程 → 输入序号 `1` → 成功进入 `[arthas@PID]$` 交互界面
- 学到的机制：attach 后 Arthas 会在被诊断进程里留一个 **3658 端口**（对讲机），进程不重启端口一直在，重复 attach 会 `skip attach` 直接连——适合线上，零侵入

**1.5 学会读三大命令的输出**
- `trace demo.MathGame primeFactors`：实时显示每次方法调用耗时；`[throws Exception]` 表示这次调用抛异常了，下面 `throw:...` 是具体错误。→ 对应需求文档"接口3"
- `monitor -c 3 demo.MathGame primeFactors`：每 3 秒出一张统计表（total/success/fail/avg-rt/fail-rate）。观察到 total≈3（因为 1 秒调用 1 次 × 3 秒），fail-rate 波动（因为 number 是随机的）。→ 对应需求文档"接口4"
- `watch demo.MathGame primeFactors '{params, returnObj}' -x 2 -n 4`：捕获"进"（参数）和"出"（返回值）；`AtExit`=正常退出、`AtExceptionExit`=异常退出。→ 对应需求文档"接口1/2"
- `jad demo.MathGame`：反编译看源码，验证类和方法真实存在，理解为什么传负数会抛异常

**1.6 关键认知**
- **不需要会 Java**：Arthas 的价值就是"不懂代码也能看懂体检报告"
- `trace 类名 方法名` 的类名要用**全限定名**（包名.类名，如 `demo.MathGame`）
- 需求文档完整链路 = 接口1 watch 抓慢 URI → 接口2 watch 反查类和方法 → 接口3 trace 深挖 → 接口4 monitor 排 Top5，底层全是 trace/monitor/watch 三个命令

---

### 阶段 2 前置：K8s 集群连接配置（2026-08-03）

**做了什么**
- 决定方案：**本地装 kubectl + 从虚机拿 kubeconfig**（而不是 SSH 绕路）
- 理由：虚机是 VMware host-only+NAT，NAT 借用主机网络，所以**虚机能连的集群，本地大概率也能连**；且后面本地写 Python 服务直接调本地 kubectl 最顺
- 本地用 winget 装 kubectl
- SSH 到虚机读 `~/.kube/config`，原样复制到本地 `C:\Users\huaji\.kube\config`

**踩坑：集群刚开机 kubectl 连不上**
- 症状：`kubectl get nodes` 报 `connectex ... actively refused`（TCP 被拒）
- 排查：SSH 进虚机，发现 6443 在监听、虚机本地 `curl 127.0.0.1:6443/healthz` 返回 ok——集群本身是好的
- 结论：集群**刚开机**，kube-apiserver 还在初始化，端口监听但外部握手没就绪
- 等待几分钟后：TCP 通了（`Test-NetConnection ... -Port 6443` = True），`kubectl get nodes` 成功
- **学会判断**：连接被拒/握手超时 ≠ 配置错误，可能是服务启动期的暂时现象

**当前集群信息（重要）**
- 集群类型：kubeadm 搭建的标准集群（不是 k3s，虽然 crictl 报错误导过我们）
- master = 192.168.56.10（control-plane，v1.28.2）✅ Ready
- worker = 192.168.56.20（v1.28.2）⚠️ 未开机（所以 NotReady，正常）
- 命名空间已有：app、default、kube-flannel、kube-system 等
- 本地 kubectl 已能正常操作集群

---

### 阶段 2：理解 K8s 手动注入 Arthas（2026-08-03）

**核心认知：注入 = 阶段 1 学到的 + kubectl 命令**
```
① kubectl cp 把 arthas 拷进 Pod 的 /tmp/arthas（拷文件）
② kubectl exec 进 Pod 运行 java -jar arthas-boot.jar（启动）
③ 输入序号 1 attach 到 Pod 内 Java 进程（选进程）
```
需求文档 3.1.4 说的"拷贝→启动→模拟输入序号1"就是这个。

**第一步：部署一个 Java "病人" Pod（math-game）**
- 发现集群里没有现成 Java 应用（todo-backend 是 http-echo，nginx-test 是 nginx）
- 所以自己构建镜像部署 math-game：
  - Dockerfile：`FROM eclipse-temurin:8` + COPY math-game.jar + CMD java -jar
  - 在虚机上 `docker build` 构建镜像
  - `docker save` → `sudo ctr -n k8s.io images import` 导入 kubelet 的 containerd（Docker 和 containerd 是两套运行时）
  - 写 Deployment YAML 用 kubectl apply 部署

**K8s 调度：污点（taint）导致的 Pending**
- 现象：业务 Pod 全部 Pending，`kubectl describe pod` 显示 `untolerated taint {node-role.kubernetes.io/control-plane}`
- 原因：master 有 kubeadm 默认的控制面污点，普通业务 Pod 不调度上去；worker 没开是 unreachable
- 解决：`kubectl taint nodes master node-role.kubernetes.io/control-plane-` 去掉污点
- 学会：Pod Pending 时用 `describe pod` 看 Events 定位原因

**踩坑：镜像构建与拉取（超多坑，都很典型）**
1. 镜像加速器 `docker.xuanyuan.me` 返回 429（挂了）→ 虚机拉不到基础镜像
2. 虚机是 NAT 模式，**不走主机上的梯子**（梯子开在 Windows 主机）→ 直连 Docker Hub 超时
   - 解决：给虚机 Docker 配置走主机代理 `http://192.168.56.1:7897`（cutecloud 的代理端口 7897，监听 0.0.0.0 允许局域网访问）
   - 修改 `/etc/systemd/system/docker.service.d/http-proxy.conf` 加 HTTP_PROXY/HTTPS_PROXY，重启 docker
3. Docker Hub 已废弃 `openjdk` 官方镜像（not found）→ 改用 `eclipse-temurin`（Adoptium，还在维护）
4. JRE vs JDK 坑在 Pod 里重现：`eclipse-temurin:8-jre` 没有 attach 能力 → 换 `eclipse-temurin:8`（JDK 版）

**踩坑：Windows 上 kubectl cp 的盘符冒号问题**
- 本地绝对路径 `c:\Users\...` 里的盘符冒号 `c:` 与 `pod:path` 格式冲突
- 报错 `one of src or dest must be a local file specification`
- 解决：用相对路径（先 cd 到目录，再 `kubectl cp arthas pod:/tmp/`）

**成功注入 + 诊断（阶段 2 核心成果）**
- `kubectl cp arthas default/math-game-xxx:/tmp/` → Pod 里 /tmp/arthas
- `kubectl exec -it <pod> -- java -jar /tmp/arthas/arthas-boot.jar` → 列进程 → 输 1 → 进入 `[arthas@1]$`
- 在 Pod 里执行 `trace demo.MathGame primeFactors` / `monitor -c 3`，结果和本地一致 ✅
- **已经能对集群里的 Java 应用手动注入 Arthas 并做实时诊断了**

---

### 阶段 3：Python 封装 kubectl（注入三步）（2026-08-03）

**里程碑：集群从 2 核升到 4 核**
- 诊断发现 kubectl 频繁 TLS 握手超时的根因是**虚机 CPU 只有 2 核且负载 10+**（跑整个 K8s 控制面 + 业务）
- 清理：删卡死的 Terminating Pod（nginx-test/todo-backend）、停掉虚机上 crd-nginx/nginx-controller.py
- 治本：VMware 里把虚机加到 4 核 → kubectl 每次秒出，问题解决 ✅

**用 Python 封装 kubectl（subprocess）**
- `subprocess.run(["kubectl", ...], capture_output=True, text=True)`：在 Python 里执行 kubectl 并拿结果
- 关键认知：命令输出分 **stdout（正常）** 和 **stderr（错误）**，还有 **returncode（退出码，0=成功）**；排障时三者都要看
- 集群偶发超时 → 封装带**重试**的 `run_kubectl(args, retries=3)`，对应需求文档 4.3"失败自动重试"

**封装注入三步（injector.py）**
1. `copy_arthas_to_pod(pod)`：`kubectl cp`，用 `cwd=` 指定 arthas 父目录 + 相对路径，**避开 Windows 盘符冒号坑**
2. `start_arthas(pod)`：`kubectl exec` 启动 arthas-boot，通过 **stdin 管道喂 "1"** 自动选择进程 attach
3. attach 成功的判断：`Attach process + success` 或 `skip attach`（3658 端口已在）

**踩坑与原理**
- kubectl 语法差异：`kubectl cp` 支持 `ns/pod:路径`，但 `kubectl exec` 要用 `-n ns pod`（斜杠会被当成"资源类型"报错）
- arthas attach 成功后交互界面走 **telnet(网络)**，不再读 stdin → 管道喂 `quit` 无效，界面会一直挂着直到超时
- 解决：只喂 `1` 完成 attach，用 `Popen + communicate(timeout) + kill`，超时后杀掉 arthas-boot 即可（**attach 的成果——3658 服务端——已经留在 math-game 里了**）
- **最终：注入三步全部用 Python 自动跑通，程序自动进入 Pod 里的 `[arthas@1]$` 界面**

---

### 阶段 4 + 5 + 6：FastAPI 服务 + 4 个诊断 API + 完善（2026-08-03）

**FastAPI 是什么**：Python Web 框架，用 `@app.post("/路径")` 装饰器把函数变成 HTTP 接口。

**生成的代码文件（都在 arthas-api/）**
- `kubectl_utils.py`：`run_kubectl`（带重试的 kubectl 执行）
- `injector.py`：`copy_arthas_to_pod` + `start_arthas`（注入三步）
- `diagnose.py`：`run_arthas_command`（用 arthas-client 非交互执行诊断命令）+ 4 个诊断函数
- `app.py`：FastAPI 应用，统一返回 `{code, msg, data}`

**核心：怎么非交互执行诊断命令？**
- attach 后 Arthas 服务端在 Pod 的 `127.0.0.1:3658`
- 用 `kubectl exec pod -- java -jar /tmp/arthas/arthas-client.jar -c "命令"` 非交互执行
- 诊断命令要加 `-n` 限制捕获次数（如 `-n 5`）让命令能自己结束

**API 接口清单（对应需求文档）**
| 接口 | 路径 | 说明 |
|---|---|---|
| 注入 | `POST /inject` | 批量注入，入参 namespace + pod列表 |
| 接口1 | `POST /diagnose/slow-requests` | 慢接口查询（watch ApplicationFilterChain） |
| 接口2 | `POST /diagnose/match-method` | 匹配业务类方法（watch DispatcherServlet） |
| 接口3 | `POST /diagnose/trace` | 方法耗时栈追踪 |
| 接口4 | `POST /diagnose/monitor` | 方法性能统计 |

**踩坑**
- uvicorn 启动要 `--app-dir` 指定代码目录，否则找不到 app 模块
- PowerShell 里 `curl` 是 Invoke-WebRequest 别名，要用 `curl.exe` 或用 `Invoke-RestMethod` 测接口
- PowerShell 传 JSON 会破坏引号，用 `Invoke-RestMethod -Body '...'` 或写文件

**验证结果**
- `GET /` 返回服务说明 ✅
- `POST /inject` 返回 code=200（注入成功）✅
- `POST /diagnose/trace` 返回 code=200 + 完整 trace 输出 ✅
- 注意：接口1/2 的命令是针对 Spring Boot/Tomcat 应用（watch ApplicationFilterChain/DispatcherServlet），math-game 不是 Web 应用测不了，需要真实 Web 应用才能验证

**服务启动方式**
```
& .venv\Scripts\python.exe -m uvicorn app:app --app-dir arthas-api --host 127.0.0.1 --port 8000
```
接口文档：http://127.0.0.1:8000/docs

---

### 补全：需求对齐（2026-08-03，用户手头有事，AI 代完成）

对照需求文档补齐的差距：

- **injector.py**：`copy_arthas_to_pod` 支持自定义 `copy_path`（默认 /tmp/arthas，对应需求 3.1.2）；`start_arthas` attach 失败自动重试（需求 4.3"注入失败自动重试"）
- **diagnose.py**：`run_arthas_command` 统一 30s 超时（需求 4.3）；支持 `copy_path`；新增 `has_match`（无匹配数据判断）、`extract_uris`（从 watch 输出提取 URI，供链路接口用）
- **app.py**：注入入参加 `region/cluster/account`（预留多集群/鉴权）；诊断接口带 `copy_path`；新增 `POST /diagnose/chain` 完整链路接口（需求第 5 章）；无匹配数据返回"无匹配耗时请求/方法"；单批次上限 50 Pod（需求 6 性能）
- **新增 `arthas-api/README.md`** 项目说明文档

**验证结果**（本地已测）：注入（带 region/cluster/account）code=200、无匹配提示正常、chain 接口返回空 uris/methods 正常。

**仍待验证**：接口 1/2/chain 需要真实 Spring Boot/Tomcat 应用 + 真实慢请求流量才能捕获数据。

---

## ✅ 当前已掌握的技能清单

- [x] 能启动并 attach Arthas，进入交互界面
- [x] 能读 trace / monitor / watch 输出
- [x] 能区分 JRE/JDK 问题并修复
- [x] 能识别 VS Code 环境变量缓存坑
- [x] 本地 kubectl 已连上测试集群
- [x] 能在 K8s 里部署一个 Java 应用 Pod（构建镜像 → 导入 containerd → Deployment）
- [x] 能向 Pod 手动注入 Arthas 并诊断（kubectl cp → exec → attach）
- [x] 能排查 Pod Pending（污点 taint）
- [x] 能给 Docker 配代理、处理镜像源问题
- [x] 能用 Python `subprocess` 封装 kubectl（含重试）
- [x] 能用 Python 自动完成注入三步（拷文件 + attach）
- [x] 理解 stdout/stderr/returncode、管道喂 stdin、交互式命令自动化
- [x] 能写 FastAPI 服务（路由、请求体、统一返回格式）
- [x] 能用 arthas-client 非交互执行诊断命令
- [x] 项目全流程走通：注入 API + 4 个诊断 API 均可调用

---

## 🚀 下一步计划

**项目已全流程走通 ✅**，后续可做的优化方向：
1. 用真实 Spring Boot/Tomcat 应用验证接口 1/2（慢接口查询、匹配业务方法）
2. 把 arthas 工具、诊断命令参数做成可配置（拷贝路径、超时、重试次数）
3. 鉴权/账户体系（需求文档里的"账户"入参）
4. 部署 API 服务本身到集群（Docker 化）
5. 补充测试用例
