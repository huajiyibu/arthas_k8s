# Pod注入Arthas工具及诊断命令API服务需求文档

# 1\. 需求概述

## 1\.1 需求背景

线上Java应用Pod偶发接口超时、响应延迟等性能问题，传统排查方式繁琐、效率低，缺乏标准化、自动化的线上JVM诊断手段。为实现快速定位慢接口、匹配接口对应业务方法、分析方法调用栈耗时、统计方法性能指标，需要搭建一套标准化的Pod Arthas工具注入\+自动化诊断命令调用API服务，支撑线上问题快速排查与性能分析。

## 1\.2 需求目标

1\.  提供统一API接口，支持批量向指定K8s Pod注入Arthas诊断工具，自动完成文件拷贝、工具启动初始化，无需人工干预操作。

2\.  封装四类常用Arthas诊断能力，通过标准化API调用，实现慢接口查询、接口绑定业务方法、方法耗时栈追踪、方法性能统计排序全流程自动化诊断。

3\.  统一入参、出参规范，适配多区域、多集群、多命名空间、多Pod批量操作场景，保障服务通用性与可扩展性。

## 1\.3 适用范围

适用于K8s集群内运行的Java应用Pod，支持批量Pod Arthas注入、在线JVM性能诊断、慢接口溯源、方法耗时分析等运维排查场景。

# 2\. 整体功能架构

本服务核心分为两大模块，功能流程串行联动：

1\.  Arthas工具注入模块：完成指定Pod的Arthas工具文件拷贝、进程启动、初始化适配，为后续诊断命令提供运行环境。

2\.  标准化诊断命令API模块：基于已注入的Arthas工具，依次提供慢接口查询、接口绑定业务类方法、方法耗时栈追踪、方法性能统计四大诊断能力，形成完整排查链路。

# 3\. 核心接口需求

## 3\.1 第一类：Arthas工具注入API

### 3\.1\.1 接口功能描述

批量向指定K8s集群、命名空间下的Pod拷贝Arthas工具包，默认拷贝至Pod内/tmp/arthas目录，自动执行Arthas启动命令并完成初始化交互（自动选择Java进程），确保Pod内Arthas工具可正常使用，为后续诊断命令提供基础环境。

### 3\.1\.2 接口入参

|参数名称|参数类型|是否必填|默认值|参数说明|
|---|---|---|---|---|
|区域|String|是|无|K8s集群所属区域，如华东、华北等|
|集群|String|是|无|目标K8s集群名称，用于定位集群资源|
|账户|String|是|无|集群操作授权账户，用于鉴权访问Pod资源|
|命名空间|String|是|无|目标Pod所属K8s命名空间|
|pod|List\<String\>|是|无|目标待注入的Pod名称列表，支持批量注入多个Pod|
|拷贝路径|String|否|/tmp/arthas|Arthas工具在Pod内的存放路径，支持自定义配置|

### 3\.1\.3 接口出参

|参数名称|参数类型|参数说明|
|---|---|---|
|code|Integer|状态码：200成功、500失败|
|msg|String|返回提示信息，成功返回“注入成功”，失败返回具体错误原因|
|data|Object|返回成功注入的Pod列表、工具存放路径、启动状态|

### 3\.1\.4 核心执行逻辑

1\.  基于入参的区域、集群、账户、命名空间信息，校验集群资源与Pod有效性；

2\.  批量将Arthas工具完整文件夹拷贝至目标Pod的指定路径（默认/tmp/arthas）；

3\.  进入Pod工具目录，执行启动命令 `java -jar arthas-agent.jar`；

4\.  自动识别当前Pod内Java进程，模拟人工输入序号1完成进程绑定，启动Arthas监听服务；

5\.  校验工具启动状态，返回批量Pod注入结果。

## 3\.2 第二类：Arthas常用诊断命令API

基于已注入启动的Arthas工具，封装4个阶梯式诊断接口，形成**慢接口筛选→接口绑定业务方法→方法耗时栈追踪→性能指标统计**的完整排查链路，所有命令默认跳过JDK内置方法，精准定位业务代码问题。

### 3\.2\.1 接口1：查询指定耗时以上的请求路径

#### 功能描述

监控Tomcat请求过滤链路，筛选出单次请求耗时大于指定阈值的接口路径，同时捕获请求URI、响应状态码，快速定位慢请求接口。

#### 入参

继承集群、Pod、命名空间基础参数，新增必填参数：costTime（Integer，单位：毫秒，耗时阈值）

#### 核心执行命令

`watch org.apache.catalina.core.ApplicationFilterChain doFilter '{params[0].getRequestURI(), params[1].getStatus()}' -x 2 '#cost>xxx'`

#### 出参说明

返回所有满足耗时阈值的请求数据，包含：请求URI、响应状态码、请求耗时、请求时间，自动去重汇总慢接口列表。

### 3\.2\.2 接口2：根据请求路径匹配对应业务类与方法

#### 功能描述

基于Spring MVC请求分发链路，根据指定慢接口URI，精准匹配该请求对应的业务处理控制器类名、方法名，解决接口路径无法直接对应业务代码的问题。

#### 入参

继承基础集群Pod参数，新增必填参数：requestUri（String，接口请求路径，如/api/loop）

#### 核心执行命令

`watch org.springframework.web.servlet.DispatcherServlet doDispatch '{#uri = params[0].getRequestURI(),#chain = target.getHandler(params[0]),#handler = #chain != null ? #chain.getHandler() : null,#handlerClass = #handler != null ? #handler.getClass().getName() : "null",#methodName = (#handler instanceof org.springframework.web.method.HandlerMethod) ? #handler.getMethod().getName() : "N/A",new String[]{#uri, #handlerClass, #methodName}}'  'params[0].getRequestURI().equals("/api/loop")'  -n 5 --skipJDKMethod false`

#### 出参说明

返回匹配结果：请求URI、对应业务全限定类名、业务方法名，为后续耗时追踪提供精准代码定位。

### 3\.2\.3 接口3：方法耗时调用栈追踪

#### 功能描述

基于接口2查询得到的业务类、方法，追踪方法完整调用栈，展示函数内部各子方法的执行耗时，精准定位代码瓶颈点，过滤JDK内置方法干扰。

#### 入参

继承基础集群Pod参数，新增必填参数：className（String，业务全限定类名）、methodName（String，业务方法名）、costTime（Integer，耗时阈值，单位：毫秒）

#### 核心执行命令

`trace 类名 方法名 -n 5 '#cost>1000' --skipJDKMethod false`（示例：trace rest\.example\.rest\.controller\.UserController loop \-n 5 '\#cost\>1000' \-\-skipJDKMethod false）

#### 出参说明

返回方法完整调用栈、每一级子方法执行耗时、调用次数、耗时占比，高亮展示超阈值耗时节点。

### 3\.2\.4 接口4：业务方法性能统计排序

#### 功能描述

对指定业务类下所有方法进行周期性性能统计，包含调用次数、平均耗时、最大耗时、成功率等指标，按耗时倒序排序，输出Top5耗时方法，用于批量评估接口性能问题。

#### 入参

继承基础集群Pod参数，新增必填参数：className（String，业务全限定类名）、cycle（Integer，统计周期，单位：秒，默认10秒）

#### 核心执行命令

`monitor -c 10 类名 * | grep -v timestamp | sort -k3 -nr | head -n 5`（示例：monitor \-c 10 rest\.example\.rest\.controller\.UserController \* \| grep \-v timestamp \| sort \-k3 \-nr \| head \-n 5）

#### 出参说明

返回Top5耗时方法的统计数据：方法名、调用次数、平均耗时、最大耗时、最小耗时、异常率，按平均耗时倒序排列。

# 4\. 通用约束与规则

## 4\.1 命令通用参数约束

1\.  **\-n 5**：所有诊断命令默认限制捕获5次请求，避免日志刷屏、资源占用过高；

2\.  **\-\-skipJDKMethod false**：不跳过JDK内置方法，完整展示全链路调用栈，保证排查完整性；

3\.  **\-x 2**：对象展开深度为2级，兼顾数据完整性与展示简洁性；

4\.  耗时阈值 **\#cost**支持自定义入参，适配不同慢接口排查场景。

## 4\.2 执行流程约束

1\.  必须先执行Arthas注入API，工具启动成功后，方可执行后续诊断命令API；

2\.  诊断API需按「接口1→接口2→接口3/接口4」顺序执行，后序接口依赖前序接口的输出结果；

3\.  支持单Pod、批量Pod执行，批量执行时返回每个Pod独立的诊断结果。

## 4\.3 异常处理规则

1\.  集群权限不足、Pod不存在：直接返回失败，提示资源不存在或鉴权失败；

2\.  Arthas拷贝/启动失败：返回注入失败，标注异常Pod名称与错误日志；

3\.  无满足阈值的请求/方法：返回空数据并提示“无匹配耗时请求/方法”；

4\.  命令执行超时：统一超时时间30s，超时返回执行超时提示。

# 5\. 接口调用链路

1\.  前置调用：Arthas工具注入API（完成工具部署启动）

2\.  第一步：调用【查询大于指定秒数的请求路径API】，获取慢接口URI列表

3\.  第二步：基于慢接口URI，调用【匹配业务类方法API】，获取目标类名、方法名

4\.  第三步：按需调用两大分析接口：

\- 精准溯源：调用【trace栈追踪API】定位代码耗时瓶颈

\- 批量性能评估：调用【monitor统计API】输出Top5耗时方法

# 6\. 非功能需求

1\.  兼容性：支持主流K8s版本、Java8及以上应用，兼容Spring Boot、Tomcat常规Web项目架构；

2\.  性能：单批次支持最多50个Pod批量注入与诊断，单接口响应超时≤30s；

3\.  可靠性：注入失败自动重试1次，诊断命令执行异常留存错误日志，便于问题溯源；

4\.  可扩展性：支持自定义工具拷贝路径、自定义耗时阈值、自定义统计周期，方便后续迭代扩展。
