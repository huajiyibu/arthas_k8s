# 验证状态记录（2026-08-04/05，最终版）

> 真实 Web 应用（demo-web）上各诊断接口的验证进度 + 环境坑 + 代码修复记录。

> **2026-08-05 更新**：应老师要求，服务已从 **FastAPI 迁移为 Flask**（`app.py` 重写，原版备份为 `app_fastapi_backup.py`），启动改为 `python app.py`。
> Flask 版已端到端验证：`/inject` 注入成功、`/diagnose/match-method` 匹配 `/slow → DemoController.slow`。

## 环境
- 集群：master=192.168.56.10（4核），worker=192.168.56.20，kubeadm v1.28.2，均 Ready。
- 应用：demo-web（Spring Boot 2.7 + Tomcat 8080），2 副本，含 `/fast`（即时）与 `/slow`（sleep 2s）。
- 验证用的干净 Pod：`demo-web-75bb7668b7-hldhw`（10.244.0.37，master 上，已注入 Arthas）。
- 本地 API：uvicorn 127.0.0.1:8000；端口转发 18080 → demo-web pod 8080。

## 各接口验证结果

| 接口 | 状态 | 验证方式 | 结果 |
|------|------|---------|------|
| 接口1 slow-requests | ✅ 成功 | API 端到端 + 命令层 | watch ApplicationFilterChain，捕获 `/slow` cost=2010~2218ms>1000ms，返回 200 |
| 接口2 match-method | ✅ 成功 | API 端到端 | `/slow` → `{"class":"com.example.demo.DemoController","method":"slow"}` |
| 接口3 trace | ✅ 成功 | 命令层 | trace DemoController.slow → 2000ms，栈定位到 `Thread.sleep()` |
| 接口4 monitor | ✅ 成功 | 命令层 | monitor DemoController：slow 2 次调用，avg-rt 2005.94ms，成功率 100% |
| 接口5 chain | ✅ 成功 | API 端到端 | `/diagnose/chain` 返回 `uris:["/slow"]` → `match:{"class":"com.example.demo.DemoController","method":"slow"}`，慢接口→绑方法全链路打通 |

## 代码侧修复（3 个，均已生效）

### 修复1：`match_business_method`（接口2）命令写法
- **坑**：arthas-client 的 `-c` 参数会把命令里的单/双引号都剥掉，OGNL 条件 `equals("/slow")` 被剥成 `equals(/slow)` → 语法错误。
- **坑2**：`--skipJDKMethod false` 会被 arthas 误当成"条件表达式"参数。
- **改法**：不加条件、不加 --skipJDKMethod，watch 全部 doDispatch 输出 `{请求URI, handler}`，`-n 50`，由 Python 侧按 URI 过滤。
- 新增 `filter_match_method(output, request_uri)` 解析器：真实输出格式
  `@HandlerExecutionChain[HandlerExecutionChain with [com.example.demo.DemoController#slow()] and 2 interceptors]` → 提取 `class`+`method`。

### 修复2：`run_arthas_command` 超时带回部分结果
- **坑**：watch/trace 类命令要凑满 `-n` 才退出；流量不够时挂满 30s 超时，`subprocess.run` 超时会**丢弃已捕获的部分输出**。
- **改法**：改用 `subprocess.Popen` + `communicate(timeout)`；超时后 `kill()` 并把已捕获的部分输出取回，若含真实数据(`has_match`)直接返回。

### 修复3（基础设施）：master 配置 `huaji` 免密 sudo
- 写入 `/etc/sudoers.d/huaji-nopasswd`（`huaji ALL=(ALL) NOPASSWD: ALL`），`visudo -c` 校验通过。
- 解决后台 kubectl 命令 sudo 需要 TTY 认证的问题。

## 两个环境坑（非代码问题）

### 坑1：宿主机网络不稳定（疑似"梯子"/代理）导致 kubectl 时通时断
- 现象：宿主机 kubectl 连接 apiserver 时通时断（TLS handshake timeout），/slow 等响应随机丢失；但长连接（SSH）和集群内网（master→Pod）一直稳定。
- 排查：apiserver 本机 healthz 200、负载低、conntrack 正常、TCP 到 6443 时通时断；PowerShell 无代理环境变量。
- **用户确认：宿主机的网络（"梯子" cutecloud 代理）当时确实在波动。** 结论：**根因是宿主机网络不稳**（很可能是梯子/TUN 模式干扰或网络抖动），不是 VMs、不是集群、不是代码。宿主网络稳定时 kubectl 5/5 全通、仅 0.2s。
- 应对：**验证全走 master 侧**（master 本机 kubectl + 集群内网），仅 SSH 跨宿主机；或等宿主机网络稳定后再走 API。

### 坑2：同一 Pod 反复 attach Arthas 后 JVM 卡死
- 现象：接口1 验证后继续在同一 Pod 上多次 watch/trace，之后 /slow 全部超时（HTTP 000），连 Pod 内 curl 和 kubectl exec 都卡死。
- 结论：残留 listener 累积卡死 JVM，不可逆。**正确处理：每 Pod 只做一轮验证；卡死直接 `kubectl delete pod` 重建。**
- 本次已通过 `--force --grace-period=0` 删除重建干净 Pod。

## 验证技巧（master 侧脚本，绕开抖动网络）
- 关键：`kubectl --kubeconfig=/etc/kubernetes/admin.conf`（master 上 huaji 的 kubeconfig 指向错误的 k3s 路径）。
- arthas 通过 worker 旧 Pod `/tmp/arthas` 用 `kubectl exec + tar 流式` 提取到 master，再 `kubectl cp` 进新 Pod——全程集群内网。
- 验证脚本在项目根：`verify_i1.sh`、`verify_i2final.sh`、`verify_i234.sh`、`verify_i2new_i4.sh`（CRLF 用 PowerShell `-replace "`r",""` 处理后推送）。

## 完成状态（2026-08-04 晚）
- **5 个接口全部验证成功**（换香港梯子节点后宿主机网络恢复，chain 一次跑通）。
- 项目核心功能已全部落地并验证：注入 → 慢请求监控 → 按 URI 绑方法 → 方法栈追踪 → 性能统计 → 完整链路。
