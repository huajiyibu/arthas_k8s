# 📋 实现与需求文档差异记录

> **需求文档**：`Pod注入Arthas工具及诊断命令API服务需求文档.md`
> **实现代码**：`arthas-api/`（kubectl_utils.py / injector.py / diagnose.py / app.py）
> **记录日期**：2026-08-04

本文档逐条对照需求文档，记录**当前实现与需求的差异**，便于后续迭代补齐。

---

## 一、已实现且符合需求的（✅）

| 需求点 | 实现情况 |
|---|---|
| 注入 API 批量 Pod（3.1.2） | ✅ `pod` 支持列表，不填默认注入命名空间下全部 Pod |
| 拷贝路径可自定义（3.1.2） | ✅ `copy_path` 参数生效，默认 `/tmp/arthas` |
| 自动 attach 选进程（3.1.4.4） | ✅ `start_arthas` 通过管道喂序号自动绑定 |
| 统一出参 `{code,msg,data}`（3.1.3） | ✅ 所有接口统一 |
| 4 类诊断接口（3.2） | ✅ 全部实现（slow-requests / match-method / trace / monitor） |
| 诊断接口批量执行（4.2） | ✅ `pod` 支持单个/列表/不填=全部，返回每 Pod 独立结果 |
| 链路串联（第 5 章） | ✅ 新增 `/diagnose/chain` 自动串联 接口1→2 |
| 批量上限 50 Pod（6.2） | ✅ 注入接口有 `len(pod) > 50` 检查 |
| 失败自动重试（6.3） | ✅ `start_arthas` retries=2、`run_kubectl` retries=3 |
| 无匹配数据提示（4.3.3） | ✅ 返回"无匹配耗时请求/方法" |
| 诊断命令 30s 超时（4.3.4） | ✅ `run_arthas_command` timeout=30 |
| 自定义耗时阈值/统计周期 | ✅ `cost_time`、`cycle` 均为入参 |

---

## 二、与需求文档的差异（重点 ⚠️）

### A. 需求要求、但当前实现未做到（❌）

| # | 需求点 | 需求文档原文 | 当前实现 | 差异 |
|---|---|---|---|---|
| 2 | 接口1 出参"自动去重汇总慢接口列表" | 3.2.1 出参："自动去重汇总慢接口列表" | 接口1 返回 watch **原始输出**，未结构化解析/去重/汇总 | ❌ 未实现结构化出参 |
| 3 | 接口4 出参"按耗时倒序排序，输出 Top5" | 3.2.4 出参："按平均耗时倒序排列，输出 Top5 耗时方法" | 接口4 返回 monitor **原始输出**，未排序、未取 Top5 | ❌ 未实现排序/截取 |
| 4 | 基于"区域/集群/账户"校验资源 | 3.1.4.1："基于入参的区域、集群、账户、命名空间信息，校验集群资源与 Pod 有效性" | `region/cluster/account` 仅作为字段记录，**不实际用于连接/校验** | ❌ 未实现多集群/资源校验 |
| 5 | 账户鉴权 | 3.1.2："账户…用于鉴权访问 Pod 资源" | 无鉴权逻辑 | ❌ 未实现 |
| 6 | 超时返回"执行超时"提示 | 4.3.4："超时返回执行超时提示" | 超时抛异常返回"失败"，**不是明确的"执行超时"提示** | ❌ 未区分超时提示 |

### B. 实现有差异 / 部分满足（⚠️）

| # | 需求点 | 需求文档 | 当前实现 | 差异说明 |
|---|---|---|---|---|
| 1 | 注入启动命令 | 3.1.4.3：`java -jar arthas-agent.jar` | 实现用 `java -jar arthas-boot.jar` | **命令不同**（agent 直连模式 vs boot 引导模式），都能 attach，但需确认是否符合预期 |
| 2 | 接口1/2 真实验证 | 3.2.1/3.2.2 | 命令已实现，但**未用真实 Web 应用验证** | math-game 非 Web 应用，`ApplicationFilterChain`/`DispatcherServlet` 不存在，返回"无匹配"；慢接口/匹配方法链路**未真实跑通** |
| 3 | `-n 5` 捕获限制（4.1.1） | 所有诊断默认 -n 5 | trace/watch 有 `-n 5`；**monitor 无**（monitor 用 `-c cycle` 周期，无 -n 概念） | 基本符合（monitor 语义不同） |
| 4 | `--skipJDKMethod false`（4.1.2） | 所有诊断命令带 | trace/watch 命令里有；**monitor 无该参数** | monitor 命令无此参数，不影响 |
| 5 | `-x 2` 展开深度（4.1.3） | 所有诊断命令带 | 仅 watch 命令有 `-x 2`；trace/monitor 无 | trace/monitor 无 -x 参数（不适用） |
| 6 | 出参含"启动状态" | 3.1.3 data："启动状态" | 返回 `results: [{pod, status:"注入成功"}]` | 有简单状态，但没有独立"启动状态"字段 |
| 7 | 链路 ③④ 自动串联 | 第 5 章："按需调用 trace/monitor" | `/diagnose/chain` 只串联 接口1→2；trace/monitor 需从接口2输出手动解析类方法后再调 | ⚠️ 链路只做了一半 |

### C. 超出需求 / 额外实现的（➕）

| # | 实现 | 说明 |
|---|---|---|
| 1 | `pod` 不填默认注入命名空间下全部 Pod | 增强，符合"批量"精神（需求未明确写） |
| 2 | `/diagnose/chain` 链路接口 | 把第 5 章链路做成一个接口，需求未规定接口形式 |
| 3 | `has_match` 无匹配数据判断 | 实现 4.3.3 的辅助逻辑 |
| 4 | `extract_uris` 从 watch 输出提取 URI | 供链路接口用 |
| 5 | `/docs` 在线接口文档（Swagger UI） | FastAPI 自带，额外便利 |

> **2026-08-05 框架变更**：应老师要求，服务由 **FastAPI 迁移为 Flask**（`arthas-api/app.py` 重写为 Flask 版，原 FastAPI 版备份为 `app_fastapi_backup.py`）。
> 所有接口路径、参数、`{code,msg,data}` 返回格式、批量/无匹配/超时等逻辑全部保留，并在真实 Web 应用上重新端到端验证通过（`/inject` 注入成功、`/diagnose/match-method` 匹配到 `DemoController.slow`）。
> 启动方式由 `uvicorn app:app` 改为 `python app.py`（或 `flask --app app run`）。

---

## 三、总结与补齐优先级建议

| 优先级 | 事项 | 对应需求 | 难度 |
|---|---|---|---|
| 🔴 高 | 诊断接口支持批量（pod 可选，不填=全部，返回每 Pod 独立结果） | 4.2 | 低（改动不大） |
| 🔴 高 | 用真实 Spring Boot/Tomcat 应用验证接口 1/2/chain | 3.2.1/3.2.2 | 中（需部署 Web 应用） |
| 🟡 中 | 接口1/4 出参结构化（接口1去重汇总、接口4排序取Top5） | 3.2.1/3.2.4 出参 | 中 |
| 🟡 中 | `region/cluster/account` 实际生效（多集群选择/鉴权） | 1.2.3 / 3.1.2 | 中高 |
| 🟢 低 | 明确"执行超时"提示、注入启动命令统一为 agent.jar、补"启动状态"字段 | 4.3.4 / 3.1.4 / 3.1.3 | 低 |

---

## 四、附：需求文档章节对照速查

- **第 1 章 概述** → 目标/范围已理解 ✅
- **第 2 章 架构** → 注入模块 + 诊断模块 ✅
- **第 3.1 注入 API** → 主体已实现，资源校验/鉴权未实现 ⚠️
- **第 3.2 诊断 API** → 4 接口已实现，出参结构化待补 ⚠️
- **第 4 章 通用约束** → 命令参数基本符合，批量诊断未实现 ⚠️
- **第 5 章 调用链路** → chain 接口已做一半 ⚠️
- **第 6 章 非功能** → 批量50/重试/自定义已实现 ✅
